<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Application Routes
|--------------------------------------------------------------------------
|
| Here is where you can register custom routes for your application.
| The Base Tenant package handles authentication and core routes:
|
| - GET /               → Redirects to login (guests) or dashboard (authenticated)
| - GET /dashboard      → Dashboard (requires auth)
| - GET /login          → Login page
| - GET /register       → Registration page
| - And more...
|
| Add your custom application routes below.
|
*/

// Example custom route:
// Route::get('/custom-page', function () {
//     return view('custom-page');
// })->middleware(['auth']);
